<%= form_with model: @post, local: true do |form| %>
  <div>
    <%= form.label :genre %>
    <%= form.select :genre, Post.genres.keys %>
  </div>
  <div>
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>
  <div>
    <%= form.submit "送信" %>
  </div>
<% end %>